[tool.poetry]
name = "django_project"
version = "0.1.0"
description = ""
authors = ["Laila <laila.mirza@live.co.uk>"]

[tool.poetry.dependencies]
python = "^3.8"
django = "^3.0.8"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
